#include <iostream>
using namespace std;

int main()
{
    for(int index = 1; index <= 5; index++) //(variable declaration, condition, increment/decrement(gets executed at the every end of loop iteration))
    {
        cout << index << endl;
    }

    return 0;
}